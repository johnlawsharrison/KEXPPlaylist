import { AuthorSearchPipe } from './author-search.pipe';

describe('AuthorSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AuthorSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
